import 'package:flutter/material.dart';

class FocusMode extends StatelessWidget {
  const FocusMode({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('focus mdoe'),
    );
  }
}
